spring:
  datasource:
    url: jdbc:mysql://localhost:3306/csvdatabse
    username: root
    password: codezmr
    driver-class-name: com.mysql.cj.jdbc.Driver
    
  jpa:
    show-sql: true
    hibernate:
      ddl-auto: update

  batch:
    job:
      enabled: false
    jdbc:
      initialize-schema: ALWAYS